---
tags: [project, opponent-scout-hub, claude-code, jfa, サッカー]
project: Opponent Scout Hub（対戦国情報集約アプリ）
status: 稼働中（なでしこ／U-20／U-17の3カテゴリー、1日3回自動収集・自動公開）
last-updated: 2026-08-15
---

# Opponent Scout Hub — 作業ログ / 引き継ぎノート

> **このノートの使い方**: 新しいClaudeのセッション（Claude Code on the web など）で続きを再開したいときは、GitHubリポジトリ **`shotamishio/Opponent-Scout-Hub`** を開いた状態で「作業ログの続きから作業して」と伝えてください。このノートはリポジトリ内（`Opponent Scout Hub - 作業ログ.md`）にも入っているので、貼り付けは不要です。
>
> **まず読むべき場所**: 現状は第3章、次にやることは**第7章**。

---

## 1. プロジェクト概要

**依頼者**: サッカー日本女子代表（なでしこジャパン）担当のプロアナリスト。

**目的**: 対戦国各国の情報（試合結果・今後の日程・チーム状況・監督の実績や評判）を、ネットのニュース・各国協会公式サイト・SNSから集約するWebアプリを作りたい。対象カテゴリーは当初5つだったが、**現在は以下の3つ**:

- なでしこジャパン（A代表）
- U-20 日本女子代表（旧U-19の情報もここに集約）
- U-17 日本女子代表（旧U-16の情報もここに集約）

> U-19・U-16は2026-08-15に廃止し、その情報はU-20・U-17に集約している（理由は第3章-D・E）。当初は5カテゴリーで設計されていたため、`project/`内の元デザインや`chats/chat1.md`には5カテゴリー前提の記述が残っている。

情報源は信頼度別に4段階のティアで管理する設計（**T1**: 各国協会・クラブ公式／**T2**: 現地主要メディア／**T3**: 記者個人／**T4**: ファン情報）。

**デザインの出どころ**: Claude Design（claude.ai/design）というAIデザインツールで、ユーザーが対話しながら作ったモックアップ（`project/Scout Hub.dc.html`ほか）を、コーディングエージェント（このClaude）が実装するという「ハンドオフ」の流れで始まったプロジェクト。デザイン自体はすでに完成していたため、今回の作業は「このデザイン通りに、実際に動くWebアプリを作る」こと。

---

## 2. これまでの経緯（時系列）

1. **デザインバンドルの読み込みと実装方針の確認**
   `project/`フォルダ内のデザインファイル一式・`chats/chat1.md`（デザイン検討時のやりとり）・`README.md`（ハンドオフ用の指示書）を読み込み。実装前に以下を確認して合意:
   - 技術スタック: **React + Vite の SPA**（このリポジトリには元々アプリの土台が何もなかったため、ゼロから構築）
   - データ: 元デザインに埋め込まれていた**サンプル（ダミー）データをそのまま移植**（本物のニュース収集はまだ実装しない）
   - 動画→日本語要約機能: 実際のAI文字起こしは行わず、**見た目だけのシミュレーション**（元デザイン通り）

2. **アプリ本体の実装**（`app/`フォルダ、React 18 + TypeScript + Vite）
   7画面をすべて実装:
   - ホーム（対戦国一覧、均等グリッド／大会別レーンの2レイアウト、アジア公式戦の対戦国20年グリッド）
   - 国別詳細（2カラム分析／信頼度ティア並列の2レイアウト）
   - 監督プロファイル
   - 動画URL→日本語要約（シミュレーション）
   - 情報源・信頼度の管理
   - 更新フィード／アラート
   - レポート出力（PDF風プレビュー）

   状態管理はReact Context + useReducer。デザインチャットで決まった紺色（`#021443`）のアクセントカラーを、ベースの"Industry"デザインシステムの上に適用。全画面・全操作をPlaywrightで実機確認済み。

3. **「普通のホームページとして公開できるファイルに」という依頼**
   - Viteのビルド設定を調整し、相対パス（`base: './'`）でどのサーバー・どのサブフォルダに置いても表示が崩れないように対応
   - 出力を`site/`フォルダにまとめ、`assets/js`・`assets/css`・`assets/img`・`assets/fonts`と種類別に整理

4. **「国旗の画像を、取得できるサイトから探して挿入して」という依頼**
   - 元デザインは`flagcdn.com`という外部サイトから国旗画像を読み込む仕様だったが、このサンドボックス環境からは到達できなかった
   - `flag-icons`（MITライセンス、npm経由で取得）のSVGを42か国分ダウンロードし、**アプリに直接同梱**（外部通信ゼロに）

5. **フォントの自前ホスティング化**
   - Google Fonts CDN（`Barlow` / `Barlow Condensed`）への依存もなくし、`@fontsource`パッケージから該当ウェイトのWOFF2ファイルを取得してアプリに同梱

6. **重要なバグを発見・修正**
   本番ビルドを実際にサブフォルダ配置のサーバーに見立てて動作確認したところ、**多くの国旗が表示されない不具合**を発見。原因は、サイズの小さい国旗SVGが本番ビルド時にコードへ直接埋め込まれる際、画像データ内の`(` `)`記号がCSSの`url(...)`記法と衝突していたこと。`FlagSwatch.tsx`でURLをダブルクォートで囲む（`url("${src}")`）ように修正して解決。**この不具合は開発用サーバーでは再現せず、本番ビルドを実際に配置して初めて見つかったもの**。

7. **「サーバーへのアップロード方法がわからない、無償で自分で使いたい」という依頼**
   - CSS・JS・国旗画像・フォントを**すべて1つのHTMLファイルに埋め込んだ単一ファイル版**（`opponent-scout-hub.html`）を作成。ダブルクリックするだけでブラウザに表示可能、サーバー・インターネット接続とも不要
   - Claudeの**Artifact機能でも公開**（ワンクリックでブラウザ表示できるリンクを発行）:
     `https://claude.ai/code/artifact/a3417e6d-ab12-4654-82ec-2a3f42c37c1b`
     ※このリンクはこのClaudeアカウント経由でのみ有効。共有するには「共有」メニューから明示的に共有設定が必要。

8. **「本物のデータを収集するツールに」という方針転換 → 実装完了**
   「実際にプロのアナリストとして、ネットのニュース・公式サイトから対戦国情報を収集し、無料の範囲で1日3回自動更新されるサイトにしたい」という依頼。保留していた4つの論点は次のとおり決着し、実装済み（詳細は第3章）:
   - 自動更新 → **GitHub Actionsで実現**（完全無料）
   - SNS収集 → **見送り**（無料APIが実質存在しないため）
   - 試合結果・日程データ → **見送り**（信頼できる無料ソースなし）。代わりに画面から該当UIを撤去し、**本物のニュース表示に置き換え**
   - 対象範囲 → **5カテゴリー全部**（のちに3カテゴリーへ縮小。第3章-D）

9. **リポジトリをGitHubへ（永続化の完了）**
   ユーザーが `shotamishio/Opponent-Scout-Hub` を作成してpush。これによりクラウド作業環境が破棄されてもコードが失われなくなり、同時にGitHub Actions／GitHub Pagesの土台ができた。

10. **【2026-08-15】収集データの品質修正、カテゴリー整理、監督情報と協会公式情報の実データ化**
    実際に自動収集が回ったあとのデータを検証し、3段階で修正した。まず年代の取り違えと無関係な記事の混入（第3章-B）、次に公開後にユーザーの指摘で判明した**別競技（クリケット・バレー・野球・バスケ）の混入**と古い記事（第3章-C）、次に構造的に成立しないカテゴリー（U-19・U-16）の廃止と、その情報のU-20・U-17への集約（第3章-D・E）。次に、唯一サンプルデータのまま残っていた監督プロファイルをWikipediaベースの実データに置き換え（第3章-F）、次に、実際には0件だった協会公式（T1）の収集を実装し（第3章-G）、次に飾りだった「再収集」ボタンを実際に機能させ（第3章-H）、最後に「追加した国の情報が出ない・追加した国が消える」問題を修正した（第3章-J）。

---

## 3. 「本物のデータ収集」フェーズでやったこと

### A. 収集の仕組み（`collector/` + GitHub Actions）

- **収集元**: Google ニュース RSS（APIキー不要・完全無料）。各国につき**2回**問い合わせる: ①一般検索 ②**その国の協会公式サイトを名指しした検索**（第3章-G）。ソースのドメインが協会公式なら **T1**、それ以外は **T2** に自動分類する（`collector/lib/officialDomains.mjs`）。
- **T3（記者個人）・T4（ファン情報）は空のまま**。SNSの無料自動収集が実質不可能なため。UI側の枠組みは残してあるので、手動で情報源を足せば使える。
- **失敗時の扱い**: ある国の取得に失敗したら、その国だけ**前回の成功データを残し「前回収集分」と表示**する（1回の失敗で画面が空になるのを防ぐため）。
- **1日3回**: `.github/workflows/collect-and-deploy.yml` が日本時間おおよそ **09:00 / 17:00 / 01:00** に実行され、「収集 → JSONをコミット → ビルド → **GitHub Pagesへ公開**」まで自動で行う。**サーバーへの手動アップロードは不要になった**。

### B. 今回（2026-08-15）直した2つの不具合

実際に収集されたデータを1件ずつ点検して見つかったもの。どちらも「画面には出ているが、中身が正しくない」タイプで、見た目だけでは気づけない種類の不具合。

1. **年代別カテゴリーに、A代表のニュースがそのまま出ていた**（重大）
   検索キーワードが国名だけで作られており、カテゴリー（U-16〜U-20）が考慮されていなかった。結果、なでしこジャパンとU-17などで**まったく同じ記事が並んでいた**（共通の12カ国すべてで内容が一致）。アナリストが見ても年代別の情報だと誤認しかねない状態だったため、**カテゴリーごとに年代（U-17など）を含む検索**を行うよう変更。

2. **無関係な記事が混ざっていた**
   例: アメリカの欄に「フラッグフットボール」の記事、中国の欄に百科事典の項目、北朝鮮の欄に他国の記事。原因は、Googleニュースが完全一致で見つからない場合に**あいまい検索へ自動的に切り替わる**こと。加えて、FIFA表記の国名（`Korea DPR`・`China PR`）は英語メディアが使わない書き方のため、そもそもほとんど一致していなかった。
   対策として、(a) **検索名を実際に報道で使われる名前**（`North Korea`・`China`・`South Korea`）に変更、(b) 取得後に **「この国のことか」「女子の記事か」「年代が合っているか」を判定して落とすフィルター**を追加（`collector/lib/topic.mjs`）。

   **重要な方針**: フィルターに通らず0件になった国は、**無理に埋めずに「情報なし」のまま**にしてある。間違った国・年代の記事が載っているより、空欄のほうがスカウティング資料として安全なため。この結果、**特にU-19・U-16は表示件数がかなり少なくなる見込み**（そもそも該当する報道が少ない年代のため）。

   なお、この修正の効果は**実際に収集が走らないと最終確認できない**（Claude側の作業環境からはGoogleニュースへ接続できないネットワーク制限があるため）。手元では、すでに収集済みの実データ184件に対してフィルターをかけ直す形で検証している。

### C. 上記を公開後、ユーザーの指摘で判明した「別競技の混入」（同日、修正済み）

B の修正を公開したところ、ユーザーから「異なるカテゴリーのニュースが混在している」との指摘。実際の公開データを確認したところ、**年代の取り違えは解消していた**一方で、まったく別の問題が残っていた。

1. **別競技のニュースが入っていた**（最大の原因）
   - U-19の画面 → ほぼ全部が**女子クリケット**（「Australia U19 Women」「7 wickets in final T20I」など）
   - U-17の韓国 → **女子バレーボール**
   - なでしこのカナダ → **女子野球**（WBSC 女子野球ワールドカップ）
   - U-20のオーストラリア → **女子バスケットボール**

   いずれも「その国の・その年代の・女子のナショナルチーム」ではあるため、B のフィルターを正しく通過してしまっていた。**競技名がどこにも書かれていない記事も多い**（クリケットは「wickets」「T20I」などの専門用語だけで書かれる）。対策として、検索キーワードに競技名を加えたうえで、**他競技の名称と専門用語を検出して落とす**処理を追加。

2. **何年も前の記事が混ざっていた**（2013年・2014年・2018年など）
   Googleニュースの「期間指定（`when:`）」が**指定どおりに効いていなかった**ため。対策として、期間の絞り込みを検索側に任せず、**記事の日付を見て自分で足切りする**方式に変更（A代表は60日、年代別は240日）。加えて「2019 ESPYS」のように**見出しに古い年号が入っている再掲載記事**も落とすようにした。

3. **ニュースとして使えない情報源**
   風刺記事サイト、チケット転売サイト、賭けオッズ集約サイトなど。ドメイン単位で除外リストを追加。

4. **「協会公式サイトなら無条件で信頼する」という例外を撤廃**
   B では「協会公式ドメインの記事は、国名や『女子』の記載がなくても通す」という例外を入れていたが、実データで検証したところ**この例外が救った記事は1件もなく**、逆にスペイン協会（RFEF）の**男子代表**の記事をA代表画面に通していた。実測にもとづき削除。

### D. U-19・U-16カテゴリーの廃止（ユーザー判断により実施）

C の調査で、**U-19・U-16はフィルターの調整では解決できない構造的な問題**を抱えていることが判明した。AFC・FIFAが現在この年代の大会を **U-20（旧U-19）**・**U-17（旧U-16）** として実施しているため、「U-19女子」「U-16女子」という表記のサッカー記事が英語メディアにほぼ存在しない（U-19で検索すると女子クリケットばかりが出てくるのは、これが理由）。

「U-20/U-17のニュースを代わりに表示する」「空欄のままにする」「カテゴリーごと外す」の3案を提示し、**ユーザーの判断でカテゴリー自体を廃止**した。削除した箇所:

- カテゴリー定義（`app/src/types.ts` の `ModeKey`、`app/src/data/modes.ts`）
- 各カテゴリーの既定対戦国リスト（`app/src/data/defaultRoster.ts`）とアジア20年グリッド（`app/src/data/asia20.ts`）
- 収集対象（`collector/lib/roster.mjs` の `CATEGORY_COUNTRIES`、`collector/lib/topic.mjs` の `CATEGORY_TOPICS`）
- 収集済みデータ（`app/src/data/collected/u19.json`・`u16.json`）

**カテゴリーを増減させるときに触る箇所は上記の5ファイル**（`ModeKey` を直せばTypeScriptが残りを漏れなく指摘してくれる）。

### E. 廃止した年代の情報を、上位カテゴリーに取り込む（ユーザー指示により追加対応）

D でカテゴリーは廃止したが、**「U-19の記事が出た場合はU-20に、U-16の記事はU-17に収集する」**という指示を受けて追加対応した。もともと同じ大会の名称変更（AFC U-19女子選手権 → U-20女子アジアカップ、U-16 → U-17）であり、選手の世代としても同じであるため、情報を捨てずに集約する形になる。

- **検索**: U-20カテゴリーは「U-20」と「U-19」の**両方**の表記で検索する（U-17は「U-17」と「U-16」）
- **取り込み**: どちらの表記の記事も、そのカテゴリーの記事として保存する
- **対戦国も引き継ぎ**: U-19だけで監視していたヨルダン、U-16だけで監視していた台湾・インド・イランを、それぞれU-20・U-17の対戦国リストに移した（これをしないと、その国の記事は収集対象にすらならないため）

**注意点として、旧名称（U-19・U-16）の記事だけは「サッカーであること」の明示を必須にしている。** 現在「U-19女子」という表記は**女子クリケット**（U19女子T20ワールドカップ）でほぼ占められており、しかも「Three Indian-origin girls named in Australia's U19 women's squad」のように**競技名も専門用語も出てこない**記事が実在するため。旧U-19データ22件・旧U-16データ13件を新しいルールで再判定し、**クリケット等の記事が1件も混入しないこと**を確認済み。

なお、ホーム下部の「過去20年間 アジアでの対戦国」グリッドは、AFC U-19女子選手権・U-16女子選手権の対戦履歴を削除したままにしてある（U-20・U-17のグリッドとは大会年度が別々に記録されており、機械的に統合すると対戦回数が不正確になるため）。統合が必要であれば別途対応する。

### F. 監督情報の実データ化（Wikipedia + 関連記事）

**「各国の監督情報が全く反映されていない。ウィキペディア等を中心に、監督の経歴と名前が載っている記事や情報サイトを閲覧できるように」**という依頼への対応。

これまで監督プロファイルは**すべてサンプルデータ**だった。しかも質が悪く、**国コードから機械的に選んだ人名**に、架空の経歴・通算成績・勝率・戦術傾向のグラフ・「協会技術委員長のコメント」まで付いていた。実在しない情報が本物と同じ見た目で並んでいる状態で、スカウティング資料としては危険なため、**作り物は全廃**して次の構成に置き換えた。

**収集する情報（`collector/lib/wikipedia.mjs`）** — Wikipediaは女子代表を全年代・全対戦国でカバーしている唯一の無料ソース。3段階で取得する:

1. **チームの記事を特定** — 「Spain women's national football team」など。国によって `football` と `soccer` で表記が分かれる（アメリカ・カナダ・オーストラリアは `soccer`）ため候補を順に試し、リダイレクトも追う。年代別は「under-20」「under-19」の両方を試す（第3章-Eと同じ理由）
2. **記事内の基本情報（infobox）から監督名を抽出** — 実際のデータは `{{flagicon|ESP}} [[Montse Tomé|Tomé]]<ref .../>` のように装飾や脚注が絡んでいるため、そこから名前とリンク先を取り出す
3. **監督本人の記事から経歴の要約とURLを取得**

**画面に表示するもの（監督プロファイル画面）**:

- 監督名と、Wikipediaによる経歴の要約
- **「情報源を開く」パネル** — ①監督本人のWikipedia記事（経歴・実績）②代表チームのWikipedia記事（歴代監督・成績）③その監督名でのGoogleニュース検索 ④Wikipedia内検索。すべて新しいタブで開く
- **「監督に言及した記事」一覧** — 監督名でニュースを収集し、出典・日付・信頼度ティア付きで表示（架空の「評判」欄の置き換え）

**特定できなかった場合は「監督情報なし」と明示**し、理由（チーム記事が無い／記事に監督の記載が無い）まで表示する。埋めない方針は他と同じ。

**削除したファイル**: `lib/coach.ts`・`data/coaches.ts`（架空の人名リスト）・`CareerTimeline.tsx`・`HonoursTable.tsx`・`ReputationList.tsx`・`lib/hash.ts`・`ProgressBar.tsx`。

**検証**: infoboxの解析はテストで担保（`collector/wikipedia.test.mjs`）。画面はサンプル形状のデータを流し込んでブラウザで実描画を確認済み（監督あり／なしの両方）。**ただしWikipedia本体への接続もこのサンドボックスからは遮断されている**ため、実際に各国の監督名が正しく取れるかは、次回の自動収集の結果で確認が必要（第7章-1）。

### G. 協会公式サイト（T1）の収集 — 実際には0件だった問題の修正

**「各国のサッカー協会のサイトからの情報は取得できているか」**という質問を受けて公開中の実データを調べたところ、**T1（協会公式）は67件中0件**、すべてT2（一般メディア）だった。設計上はT1を表示する枠があるのに、中身が入っていない状態が続いていた。

**原因は2つ**:

1. **協会サイトを直接見に行っていなかった**。Googleニュースの一般検索に協会の発表が出てくるのを待つだけの作りで、実際には協会の発表文は通信社の記事に埋もれて上位に出てこない
2. **フィルターが協会の発表文を落としていた**。「見出しに国名が入っていること」を条件にしていたが、協会は**自分のサイトで自国名をわざわざ書かない**（「Convocatoria de la selección femenina」＝「女子代表の招集メンバー」だけで済む）

**対策**:

- **協会サイトを名指しで検索する問い合わせを追加**（`site:rfef.es ...` の形式）。各国につき「一般検索」と「協会サイト検索」の2回に分け、協会の発表が一般ニュースと席を奪い合わないようにした。**取得できた場合はT1を上位に固定表示**する
- **協会ドメインの記事に限り、国名の記載を条件から外した**（そのドメインで公開されている事実が国の証明になるため）。ただし**「女子の記事であること」の条件は維持**している（協会サイトは男子代表も同じドメインで扱うため。以前この条件まで外してスペイン男子代表の記事が混入した経緯がある）
- **協会ドメインの一覧を、現在の対戦国をカバーするよう追加**（中国・ナイジェリア・ヨルダン・ウズベキスタン・台湾・イラン・バングラデシュ・ミャンマー）。北朝鮮は公開サイトが存在しないため対象外
- **画面にも反映**: 国別詳細に「協会公式サイトを開く」リンク、監督プロファイルの情報源パネルの**先頭に協会公式サイト**を追加（就任情報はWikipediaより協会サイトのほうが早いため）

**この作業中に見つかった副次的な不具合**: スペイン語・ポルトガル語圏の協会は年代別を「**sub-17**」「**sub-20**」と表記するが、判定ロジックが英語表記（U-17／Under-17）しか知らなかった。そのままだと、これら協会の年代別発表が**年代不明としてA代表の画面に混入**する（第3章-C-1と同じ種類の不具合）。検索語・判定の両方に `sub-NN` を追加して修正済み。

### H. 「再収集」ボタンを実際に機能させる

**「再収集ボタンを押した際に、手動のタイミングで情報を更新できるように」**という依頼への対応。それまでこのボタンは**更新フィード画面へ移動するだけの飾り**で、「最終収集 08/14 07:20」の表示も固定文字列だった。

**前提となる制約**: 公開サイトは静的ファイルであり、ブラウザ側から収集プログラムを起動することはできない。またGitHub Actionsを起動するにはGitHubの認証情報が必要で、**公開ページにその情報を持たせるのは論外**（誰でも見られるため）。そこで機能を2つに分けた。

1. **「再収集」ボタン＝収集済みデータの取り込み直し**
   収集プログラムは実行のたびに結果をリポジトリにコミットしているため、**そのファイルを直接読み直せば、サイトの再ビルドを待たずに最新の内容を表示できる**。ボタンを押すと3カテゴリー分を取得し、画面をその場で更新する（ページの再読み込みも不要）。
2. **「新しい収集を実行 ↗」リンク＝収集そのものの実行**
   GitHubのActions画面を開き、「Run workflow」で即時実行できる。完了後に「再収集」を押せば結果が反映される。

**設計上の注意点**:

- **失敗しても画面は空にしない**。オフライン等で取得できない場合は「取得できませんでした。表示は前回のままです」と表示し、ビルド時に同梱したデータを表示し続ける
- 収集データはこれまで**ビルド時に固定**されていたため、React側で差し替えられるよう `state/CollectedContext.tsx` を新設し、各画面はそこから読むように変更した
- **外部から取得したデータをリンクに使うことになったため、`http`/`https` 以外のURL（`javascript:` など）をリンクにしない安全確認を追加**（`safeHref`）。同梱データにも同じ確認を通している

**検証**: ブラウザで実際にボタンを押し、①取得成功時に一覧の件数バッジ・最終収集時刻が更新されること、②通信失敗時に前回の表示が保持され、その旨が表示されることを確認済み。

### I. 初回の実収集の結果（第3章-F・Gの答え合わせ）

監督情報と協会公式収集を公開後、実際に自動収集が回った結果:

| カテゴリー | 記事 | うちT1（協会公式） | 監督を特定できた国 |
|---|---|---|---|
| なでしこ | 39件 | **24件** | 21/23 |
| U-20 | 45件 | **23件** | 18/18 |
| U-17 | 52件 | **40件** | 17/19 |

**協会公式（T1）は0件から87件になり**、監督も60カ国中56カ国で特定できた（Sarina Wiegman／Sonia Bermúdez／Arthur Elias など実在の監督名が入っている）。

**ただし1件、抽出の不具合**が見つかった。北朝鮮U-17の監督名が「`Captain             =`」という壊れた文字列になっていた。原因は、Wikipediaの基本情報欄が**1行に複数項目を並べて書かれている**場合があり、監督欄が空だと**次の項目名まで読み込んでしまう**こと。リンクやテンプレート内の `|` は区切りではない点を考慮して切り出す処理に修正し、`=` が残る値は名前として採用しないようにした（テスト追加済み）。

### J. 「追加した国の情報が出ない」「追加した国が消える」の修正

U-20カテゴリーに**イタリアを追加したが情報が出ない**という報告から、2つの別々の問題が判明した。

**1. 追加した国は、そもそも収集対象外だった**

収集プログラムの対象国リストは**各カテゴリーの初期対戦国だけ**で、アプリの「＋対戦国を追加」で選べる42カ国全部ではなかった。イタリアは初期リストに無いため**収集が一度も行われず、永久に空のまま**だった（「再収集」ボタンは収集済みの結果を読み直すだけなので、これでは何も出ない）。

対策として、**収集対象をアプリの対戦国プール42カ国すべてに拡大**した。「アプリで選べる国＝収集する国」という単純な規則にすることで、今後どの国を追加しても同じ問題は起きない。不足していた14カ国（イタリア・デンマーク・ノルウェー・オーストリア・ポーランド・アルゼンチン・パラグアイ・モロッコ・カメルーン・エクアドル・ザンビア・香港・シンガポール・グアム）の検索名・別名・協会ドメインを追加している。

対象が126通り（42カ国×3カテゴリー）に増えるため、1回の実行は**15分程度**になる見込み（公開リポジトリのため実行時間の課金はなし）。

**2. 追加した国がページを閉じると消えていた**

対戦国リストがブラウザのメモリ上にしかなく、再読み込みで初期状態に戻っていた。**ブラウザに保存する方式に変更**し、**削除するまで残る**ようにした（`app/src/state/rosterStorage.ts`）。保存データは1件ずつ検証しており、廃止されたカテゴリーや存在しない国コードが残っていても画面が壊れない。

なお保存先はブラウザのため、別のパソコンには引き継がれない（公開サイトは静的ファイルで、保存先となるサーバーが無いため）。

**検証**: ブラウザでU-20にイタリアを追加し、**再読み込み後も残ること**を確認済み。また、まだ収集されていない国の画面には「次回の自動収集で取得されます。すぐに取得したい場合は『新しい収集を実行』から」と手順を明示するようにした。

---

## 4. リポジトリ構成（現在の状態）

GitHubリポジトリ: **`shotamishio/Opponent-Scout-Hub`**

```
リポジトリのルート
├── README.md                   ← 元デザインバンドルのハンドオフ指示書（触っていない）
├── chats/chat1.md              ← 元デザイン検討時のチャットログ（触っていない）
├── project/                    ← Claude Designからの元エクスポート一式（デザインの正、触っていない）
│   └── Scout Hub.dc.html          ← デザインのマスターファイル（プロトタイプ、実装の元ネタ）
├── .github/workflows/
│   └── collect-and-deploy.yml  ← ★1日3回の自動実行（収集→コミット→ビルド→Pages公開）
├── collector/                  ← ★ニュース収集プログラム（何を集めるか変えるならここ）
│   ├── collect.mjs                ← 収集の本体（各国×各カテゴリーを順に処理）
│   ├── lib/topic.mjs              ← ★検索キーワード生成 + 無関係な記事を除くフィルター
│   ├── lib/roster.mjs             ← 収集対象の国一覧・検索名・別名（app/src/data と手動で同期）
│   ├── lib/officialDomains.mjs    ← 協会公式ドメイン一覧（T1判定に使用）
│   ├── lib/wikipedia.mjs          ← ★監督情報の取得（Wikipedia）
│   └── collect.test.mjs           ← テスト（`npm test`。ネットワーク無しで動く）
├── app/                        ← ★Reactアプリのソースコード（画面を直すときはここ）
│   ├── README.md                  ← 開発・公開手順の説明（日本語）
│   ├── src/
│   │   ├── data/                     ← 対戦国・カテゴリー・監督名などの基礎データ
│   │   ├── data/collected/*.json     ← ★収集された本物のニュース（自動生成。手で編集しない）
│   │   ├── lib/collectedData.ts      ← 上記JSONの読み込み口
│   │   ├── state/                    ← 画面遷移・フィルタ等の状態管理
│   │   ├── components/screens/       ← 画面ごとのUI（home/country/coach/video/sources/feed/report）
│   │   ├── components/shell/         ← サイドバー・ヘッダー
│   │   ├── styles/                   ← デザインシステムCSS・紺色アクセント・自前フォント定義
│   │   └── assets/flags/, assets/fonts/  ← 同梱した国旗SVG・フォントファイル
│   └── package.json
├── site/                       ← ビルド出力（自動生成のためGitには入れていない）
└── opponent-scout-hub.html     ← 単一ファイル版（※下記の注意を参照）
```

> **`opponent-scout-hub.html` についての注意**: これは「本物のデータ収集」に切り替える**前**に作った単体版で、中身は**サンプル（ダミー）データのまま**です。ダブルクリックで開けるという利点はありますが、単体のHTMLは自動更新できない（＝今の運用と両立しない）ため、現在は**更新されていません**。今後どうするかは第6章を参照。

### 再開時によく使うコマンド

```bash
cd app
npm install       # 初回のみ
npm run dev       # 開発中のプレビュー（http://localhost:5173 など）
npm run build     # site/ フォルダを最新化
npm run preview   # site/ の内容をローカルで最終確認

cd ../collector
npm install       # 初回のみ
npm test          # 収集ロジックのテスト（ネットワーク不要）
npm run collect   # 手元で実際に収集してみる（要インターネット接続）
```

---

## 5. 主要な技術的決定の理由（再検討不要な事項）

- **React + Vite + TypeScript**を選定（このリポジトリには元々土台がなかったため）
- 元プロトタイプのサンプルデータ生成ロジック（国コードからハッシュ値を作り、決定論的に試合結果等を生成する仕組み）を、ほぼそのまま`app/src/lib/`に移植ずみ
- 国旗・フォントは**両方とも外部CDNをやめ、アプリに同梱**（オフラインでも確実に表示させるため）
- ビルド設定は相対パス化・アセット種類別フォルダ分けを実施済み（`app/vite.config.ts`）
- 「情報源・信頼度」「更新フィード」「レポート出力」など、本物のデータ収集を前提としたUIの枠組みは元デザインの時点でできていたため、**中身のデータを本物のニュースに差し替えるだけで済み、UIは作り直していない**（この差し替えは完了済み）
- **収集プログラム（`collector/`）はTypeScriptを使わず素のJavaScriptで書いてある**。GitHub Actions上でビルド手順なしにそのまま実行でき、自動実行が壊れにくいため。その代わり、対戦国リストは `app/src/data/` と `collector/lib/roster.mjs` の**2か所を手で同期する**必要がある（国を追加したら両方に足す）
- **収集できなかったものは、埋めずに空欄にする**。サンプルデータで埋めると「本物かサンプルか」が画面上で区別できなくなり、スカウティング資料として危険なため
- **監督情報はWikipediaから収集**（第3章-F）。経歴の要約と、本人・チームの記事へのリンク、監督名を含むニュース記事を表示する
- **動画URL→日本語要約は今も見た目だけのシミュレーション**（元デザインどおり。実際のAI文字起こしは未実装）

---

## 6. 永続化について（✅ 解決済み）

以前このノートに書かれていた「GitHubのリモートが未設定で、作業環境が破棄されるとコードが失われる」という問題は、**解決済み**です。コードはすべて GitHub（`shotamishio/Opponent-Scout-Hub`）にあり、クラウド作業環境が破棄されても失われません。

ただし引き続き注意が必要な点:

- **`site/` はGitに入れていません**（ビルドのたびに自動生成されるため）。バックアップは不要です。
- **`opponent-scout-hub.html`（単一ファイル版）は、サンプルデータのまま更新が止まっています**。今後の扱いは3案:
  1. **削除する**（推奨）— 自動更新される公開サイトがある以上、古いダミーデータのファイルが残っていると、どちらが本物か分からなくなる
  2. そのまま残す（オフラインでも「画面の見た目」を確認できる用途に割り切る）
  3. 収集済みの本物データを埋め込んだ単一ファイル版を、自動で作り直すようにする（ワークフローに1手順追加すれば可能）

  → **ユーザーの判断待ち**。

---

## 7. 次にやること（再開ポイント）

### 7-1. まず確認すべきこと（次回のワークフロー実行後）

第3章-Cの修正は、**実際に自動収集が1回走ってから結果を確認する**必要がある。確認の観点:

- サッカー以外の競技（クリケット・バレー・野球・バスケ）が消えているか
- 何年も前の記事が消えているか
- 各画面に、**その年代の記事だけ**が出ているか
- **逆に、消えすぎていないか**（＝フィルターが厳しすぎて、本来必要な記事まで落としていないか）
- **監督名が正しく取れているか**（第3章-F。Actionsのログに `head coaches identified` として「何カ国で特定できたか」が出る。取れない国が多い場合は `collector/lib/wikipedia.mjs` のチーム記事名の候補を見直す）
- **協会公式（T1）が実際に取れているか**（第3章-G。Actionsのログに `N from federation sites` と出る。**0件のままなら協会ドメインの記載が間違っている可能性**が高いので、`collector/lib/officialDomains.mjs` を実際のサイトで確認して修正する。この一覧は未検証の記載を含む）

調整が必要な場合は `collector/lib/topic.mjs` を直す。各国のJSONには `filteredOut`（フィルターで落とした件数）を記録してあるので、「落としすぎ」かどうかはそこで判断できる。

### 7-2. ✅ 決着済み: U-19・U-16カテゴリーの廃止と、上位カテゴリーへの取り込み

「該当するサッカー報道がほとんど存在しない」問題は、**カテゴリー自体は廃止し、記事はU-20・U-17に取り込む**形で決着した（経緯と作業内容は第3章-D・E）。現在のカテゴリーは**なでしこ／U-20／U-17の3つ**で、情報は取りこぼさない。

### 7-3. 検討中の候補（どれもユーザーの判断待ち）

- **T3・T4（記者個人・ファン情報）の扱い** — 自動収集は無料では不可。「手動でリンクを登録する」機能を付けるかどうか
- **試合結果・日程データ** — 一度見送った論点。改めて有料APIを含めて検討するかどうか
- **単一ファイル版の扱い** — 上記6章のとおり
- **日本語での表示** — 現在、収集される記事タイトルは英語のまま。日本語に訳して表示するかどうか（翻訳APIは有料になる可能性が高い）
